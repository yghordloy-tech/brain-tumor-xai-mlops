"""
train.py — Main training entry point.

Usage:
    python scripts/train.py --config configs/config.yaml
"""
# TODO: Implement CLI entry point
# 1. Parse args / load config
# 2. Set seeds, resolve device
# 3. Build dataloaders
# 4. Build model, optimizer, scheduler, loss
# 5. Initialise W&B run
# 6. Launch training loop via Trainer
# 7. Save best checkpoint & log to W&B Artifacts
