# TODO: Implement Pydantic request / response schemas
# - PredictionRequest (file upload)
# - PredictionResponse (class_name, confidence, probabilities)
# - ExplanationResponse (prediction + base64 heatmap image)
# - HealthResponse (status, model_loaded, version)
