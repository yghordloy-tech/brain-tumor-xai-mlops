# TODO: Implement API route handlers
# - POST /predict          — upload MRI image, return predicted class + confidence
# - POST /predict/explain  — upload MRI image, return prediction + GradCAM overlay
# - GET  /health           — liveness / readiness probe
# - GET  /model/info       — return model metadata (architecture, classes, version)
